# Path Of Exile Build Workspaces

Create one folder per concrete Path Of Exile client build:

```text
builds/<build>/<binary>/<intent>/
```

Use `builds/<build>/investigations/<topic>/` for active handovers and candidate hunts.

Do not add a build folder until the note can record the client build/title, target binary, and evidence source used to start the investigation.
